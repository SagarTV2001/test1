#include "calculator.h"
#include "integration.h"
#include <stdio.h>

int main() {
    printf("Mul: %d\n", mul(2, 3));
    printf("Div: %d\n", div(10, 2));
    printf("Sum: %d\n", add_results(1,2,3,4));
    return 0;
}
